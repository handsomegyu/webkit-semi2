server:
  port: 8080  # ?? ?? ??

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/character_shop?serverTimezone=Asia/Seoul
    username: scott
    password: tiger  # ??? MySQL ????? ??
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ??? ?? ????
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  security:
    user:
      name: admin
      password: admin123  # ?? ??? ?? ??
